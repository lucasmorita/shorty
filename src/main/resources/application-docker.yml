spring:
  datasource:
    username: postgres
    password: admin
    url: jdbc:postgresql://postgres.local.shorty:5432/postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop